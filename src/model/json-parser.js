//import { geojson } from '../assets/xycrd_sample.json'

export class JsonParser {
  constructor(){
  }

  loadfile(){

    //let res = JSON.parse(geojson)
    //console.log(geojson)

    //let data = await loadJsonFile(filePath)
    //return data
  }
}
